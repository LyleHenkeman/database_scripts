<Query Kind="SQL" />

--Finding out the SQL password

--If you ever want to find out a SQL password, use the web.config for the web service, it is generally written in plan text in there.
--Or if on a SUMO based system you can check the connection string in the mediator config GUI.